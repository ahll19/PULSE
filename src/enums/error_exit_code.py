from enum import Enum


class ErrorExitCodeEnum(Enum):
    # Here we should list all exit codes that are not correct exits. Example:
    # timeout = 420
    # if a timeout corresponds to an exit code of 420
    pass
